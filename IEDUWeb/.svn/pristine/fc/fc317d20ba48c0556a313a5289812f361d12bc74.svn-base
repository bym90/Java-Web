package com.iedu.reboard;

import java.io.IOException;

import javax.servlet.ServletException;
import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;

import com.iedu.common.IEDUMain;

public class BoardWriteForm implements IEDUMain {
	public String ieduExe(HttpServletRequest req, HttpServletResponse resp) throws ServletException, IOException {
		// 할 일 x 일반적인 폼만 보여주면 되므로!!
		// 원래 실무에서는 이런경우에는 굳이 MVC를 사용하지 않고
		// http://localhost:8080/IEDUWeb/IEDU/ReBoard/BoardWriteForm.jsp
		// 의 방식으로 직접 jsp를 호출한다.
		return "../IEDU/ReBoard/BoardWriteForm.jsp";
	}

}
